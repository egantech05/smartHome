import { StyleSheet, Text, View } from 'react-native';

export default function Temperature(){
    return (
        <View>
            <Text>
                Temperature
            </Text>
        </View>
    );
};